name: kraken
description: Plugin for Core Lightning that helps analyze invoice payments.
version: 1.0.0
homepage: https://github.com/dart-lightning/kraken

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  clightning_rpc: ^0.0.2-beta.4
  cln_common: ^0.0.1-beta.2
  cln_plugin_api: ^0.0.1-beta.2
  json_annotation: ^4.5.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  build_runner: ^2.0.0
  json_serializable: ^6.2.0
